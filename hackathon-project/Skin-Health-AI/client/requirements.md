## Packages
react-dropzone | High quality drag-and-drop file uploading for the image scanner
framer-motion | Smooth, premium page transitions and staggered animations

## Notes
Backend endpoint POST /api/scans expects a JSON body with `image` as a base64 encoded string (data URL format is assumed supported).
